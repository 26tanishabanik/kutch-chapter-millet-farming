# File to run the model prediction

def run(polygon):
	"""Takes a geojson file,  then calls get_data function 
	from another chapter with geojson file to obtain the data
	to feed to the model. The trained model runs the prediction
	then return the prediction results.
    Needs to be implemented."""
	result = "Pearl millet (Pennisetum glaucum)" # need to make it model prediction
	return result